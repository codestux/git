# Configurando servidor remoto

Devemos criar uma pasta em um local escolhido e executar os comandos abaixo dentro da pasta criada.

```bash
git init --bare
```

O parâmetro **--bare** indica que é um repositório puro e que não tem cópias físicas dos arquivos, apenas as alterações.
